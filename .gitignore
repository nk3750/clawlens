# OS
.DS_Store

# Node
node_modules/
## dashboard/dist/ is committed for plugin deployment
!dashboard/dist/
.env
.env.*

# Python
__pycache__/
*.pyc
.venv/
venv/

# IDE
.idea/
.vscode/
*.swp

# Claude Code local settings (contains tokens)
.claude/settings.local.json
.mcp.json

# Orchestrator scratch (design bundles, render probes) — never commit
.scratch/

# Playwright MCP per-session snapshots + ad-hoc screenshots from review walks
.playwright-mcp/
agent-grid-polish-review.png
