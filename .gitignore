# OS
.DS_Store

# Node
node_modules/
## dashboard/dist/ is committed for plugin deployment
!dashboard/dist/
.env
.env.*

# Python
__pycache__/
*.pyc
.venv/
venv/

# IDE
.idea/
.vscode/
*.swp

# Claude Code local settings (contains tokens)
.claude/settings.local.json
.mcp.json

# Orchestrator scratch (design bundles, render probes) — never commit
.scratch/

# Brainstorming visual companion — session mockups
.superpowers/

# Playwright MCP per-session snapshots + ad-hoc screenshots from review walks
.playwright-mcp/
*-review.png

# Release smoke artifacts (npm pack tarballs, walkthrough screenshots) — these
# regenerate every release cycle and the Obsidian Git vault-backup will sweep
# them in if left lying around. Keep them out of history.
nk3750-openclaw-clawlens-*.tgz
*-smoke-*.png
v[0-9]*.[0-9]*.[0-9]*-*.png
